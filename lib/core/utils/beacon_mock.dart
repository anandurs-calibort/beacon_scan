import 'package:flutter/material.dart';

/// Mock static beacon coordinates for demo (later replaced by API)
final Map<String, Offset> mockBeaconPositions = {
  'BEACON_A': const Offset(0, 0),
  'BEACON_B': const Offset(4, 0),
  'BEACON_C': const Offset(2, 3),
  'BEACON_D': const Offset(5, 2),
};
